name: kgraphplanner
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.12
  - pip
  # Keep this list in sync with [project].dependencies in pyproject.toml
  - pip:
    - vital-ai-vitalsigns>=0.1.55
    - vital-ai-aimp>=0.1.16
    - vital-ai-haley-kg>=0.1.25
    - langchain-core>=1.4.7
    - langchain-openai>=1.3.0
    - langchain-anthropic>=1.4.6
    - langgraph>=1.2.5
    - langgraph-checkpoint>=4.1.0
    - redis>=5.0.0
    - langsmith>=0.8.11
    - weaviate-client>=4.10,<5
    - langchain-weaviate>=0.0.6
    - langchain-text-splitters>=1.0.0
    - langchain-community>=0.4.0
    - httpx>=0.27
    - python-dotenv
    - rich>=13.7.1
    - pyyaml
    - vital-agentbox[client]>=0.1.4
    - vital-graph[client]>=0.0.38
    - deepagents>=0.6.11
    # Development dependencies — see [project.optional-dependencies].dev
    # - twine
    # - setuptools
    # - pygraphviz
    # - nest-asyncio
    # - pyppeteer
    # - build
